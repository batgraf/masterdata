# Opcjonalnie: gdy ustawiony, aplikacja używa PostgreSQL zamiast pliku JSON.
# Import: wgrywanie plików JSON i XML (mapowanie suuhouse) do bazy.
# Eksport: Pobierz bazę → JSON w tej samej strukturze co wejściowy.
# DATABASE_URL=postgresql://user:password@host:5432/dbname
